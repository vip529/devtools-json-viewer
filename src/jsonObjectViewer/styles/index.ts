const primitiveValueContainer = {
  marginLeft: 10
};

const trunkatedParent = {
  display: "flex"
};

const trunkatedValue = {
  // marginLeft: 10
};

const expandCollapseButton = {
  marginRight: 10,
  display: "inline",
  width: 20,
  height: 20
};

const jsonRoot = {
  display: "flex",
  flexDirection: "column" as "column"
};

const jsonEntry = {
  marginTop: 5,
  marginBottom: 5
};
export {
  primitiveValueContainer,
  expandCollapseButton,
  trunkatedParent,
  trunkatedValue,
  jsonRoot,
  jsonEntry
};
